# 共同富裕感知实验驾驶舱 · 产品需求文档（PRD）

| 项目 | 内容 |
| --- | --- |
| 文档名称 | 共同富裕感知实验驾驶舱 产品需求文档 |
| 版本 | v1.0（基于 Demo 逆向分析稿） |
| 编制日期 | 2026-09-16 |
| 分析对象 | `共同富裕感知实验驾驶舱-Demo_副本`（`src/Dashboard.tsx` 1367 行 / 82.8KB、`src/index.css` 135 行、`src/main.tsx`） |
| 关联资料 | 项目内《数据指标来源说明.md》；同级目录《功能清单（对标验收）（数据采集系统二期）.xlsx》；上游系统 `Simulation Experiment Management System`（沙盘实验后台管理系统） |
| 产品定位 | 只读数据分析端（驾驶舱）；数据链路：后台管理系统配置实验 → 沙盘实验端执行 → 结果回传后台 → 驾驶舱读取展示 |

---

## 一、产品概述

### 1.1 定位与边界

驾驶舱是**只读分析端**，面向"共同富裕感知实验"的成果呈现与解读。它不产生实验数据、不修改实验配置、不接收参与者输入。

- 做：聚合展示实验过程与结果数据、多维度对比、异常预警、生成分析报告。
- 不做：实验创建/配置/启停、问卷设计、用户与智能体档案维护、AR 设备管理（均由后台管理系统负责）。
- 唯一例外：洞察页对预警记录的"处理"动作会回写处理状态（当前 Demo 未实现回写）。

### 1.2 系统上下文

```
[后台管理系统]  配置实验/社区样板/智能体/问卷  ──参数下发──▶  [沙盘实验端]
       ▲                                                          │
       │                                                   实验者(智能体/真人)执行
       └──────────── 实验结果与过程记录回传 ◀──────────────────────┘
       │
       └──▶ [驾驶舱（本产品）] 只读拉取，展示总览/社区画像/实验画像/智能体/洞察
```

三类实验（后台 `category` → 驾驶舱展示名）：

| 后台 category | 后台/驾驶舱展示名 | 编号前缀 | 参与者 | 数据来源标签 | 核心结果指标 |
| --- | --- | --- | --- | --- | --- |
| `spatial` | 协商博弈 | SP | 智能体（+内部视角真人） | 智能体多轮博弈 | 支持率 |
| `charity` | 行为决策 | CW | 真人 | 真人输入 | 捐赠意愿率 |
| `insurance` | 意愿采集 | YL | 真人 | 真人输入 | 参保意愿比例 |

### 1.3 用户角色与核心场景

| 角色 | 场景 | 关注点 |
| --- | --- | --- |
| 决策/管理领导 | 大屏汇报、政策效果说明 | 总量、回收进度、结论与建议 |
| 研究人员 | 实验过程与结果解读、群体差异分析 | 逐轮趋势、分群体指标、质性发言、组间对照 |
| 运营人员 | 实验运行监控 | 实时回传、回收率、异常预警处理 |
| 技术/验收方 | 功能对标验收 | 五大验收点（见第九章追踪矩阵） |

### 1.4 设计风格基线（Demo 已确立，需继承）

深色"指挥台"风格：深空蓝底色（`#0a1628`）+ 青瓷青蓝主色（`#4cc9f0`）+ 琥珀金强调色（`#ffc857`）+ 珊瑚红告警（`#ff7a6b`）；切角面板（`.panel` 四角括号）、扫描线、网格底纹、数字等宽字体（`.num`）、KPI 辉光。所有页面必须保持同一视觉体系。

---

## 二、现状分析（Demo 逆向结论）

### 2.1 技术现状

- 技术栈：Vite 6.3.5 + React 18.3.1 + TypeScript 5.5 + Tailwind CSS 4.1.12（`@tailwindcss/vite`）+ lucide-react 图标；dev 端口 5174；`index.html` 带 `robots=noindex,nofollow`。
- 结构：**单文件页面组件**（`Dashboard.tsx` 内含 15 组 mock 常量 + 8 个通用组件 + 6 个页面组件），无路由（`useState` 切页）、无状态管理、无请求层（零 `fetch`）。
- 数据：全部为文件内 mock；《数据指标来源说明.md》已定义全部指标口径与数据链路，但与代码存在若干口径漂移（见 2.3）。
- 响应式：以 `grid-cols-2 lg:grid-cols-4`、`lg:col-span-*`、`min-w-[760px]` 横向滚动等方式做桌面优先自适应；`prefers-reduced-motion` 已降级动画。
- 已有能力：实时回传播报（CSS 无缝滚动）、KPI 带迷你趋势线、SVG 折线图/条形/环图/关系网络图（均自绘，未引入图表库）、报告生成模拟流式进度。

### 2.2 页面与能力清单（现状）

| 导航 | 页面 | 主要板块 | 状态 |
| --- | --- | --- | --- |
| 总览 | OverviewPage | 实时回传播报条、4 张 KPI 卡、实验运行全景表、实验参与与回收情况 | 已实现 |
| 社区画像 | CommunityPage | 社区切换、人口结构、公共服务设施构成、民生福祉、环境治理 | 已实现（1 个社区） |
| 实验画像 | ExperimentPage | 实验切换、生命周期时间轴、协商博弈（本场/历史汇总）、行为决策、意愿采集、对比分析 | 已实现 |
| 智能体 | AgentPage | 智能体档案表、互动网络、满意度追踪、行为模式·立场演变 | 已实现（缺资源流动） |
| 洞察 | InsightPage | 异常预警与提示、实验分析报告一键生成 | 已实现（报告仅 4 节） |

### 2.3 需求漂移与缺口清单（分析所得，需在本版需求中收敛）

| 编号 | 问题 | 说明文档口径 | Demo 实现 | 需求处理 |
| --- | --- | --- | --- | --- |
| GAP-01 | 总览底部面板数量 | 两个面板：①实验类型最新结果对比 ②真人实验结果回收情况 | 仅有 1 个"实验参与与回收情况" | FR-OV-05 保留并要求补齐① |
| GAP-02 | 行为决策分组口径 | 用"是否展示他人选择"开关区分实验组/对照组 | 用 3 个内部小实验 a/b/c（含"无告知基线"） | FR-EX-03 统一为"实验版本（versionId）+ 分组（group）" |
| GAP-03 | 跨社区横向对比 | 预留说明，新增社区后自动开放 | 仅行为决策面板一句提示，无独立对比视图 | FR-EX-06 定义为可自动开放的对比模块 |
| GAP-04 | 智能体资源流动 / 获得感 | "资源获取"为档案字段；功能清单要求"资源流动" | `AGENT_RESOURCE_TREND` 已定义但**未渲染** | FR-AG-02/FR-AG-05 |
| GAP-05 | 智能体标签 | 意见领袖/立场坚定/可说服，后台自动打标 | 仅在立场演变结论文案中出现 | FR-AG-04 |
| GAP-06 | 智能体参与历史 | 档案"实验"列悬停显示参与历史 | 未实现 | FR-AG-03 |
| GAP-07 | 质性信息（代表性发言） | 理由归集与代表性发言（按立场分组原文摘录） | 仅一条结论文字，无发言列表 | FR-EX-02 |
| GAP-08 | 报告结构 | 七节标准结构 | 仅 4 节（概况/发现/异常/结论） | FR-IN-03 |
| GAP-09 | 意愿采集金额类指标 | 总缴纳保费、总投保金额、平均可接受保费 | 仅有"意愿保费中位数/可接受保费均值" | FR-EX-04 |
| GAP-10 | 目标样本数 | 回收率 = 有效样本 / 配置目标样本数 | 后台 `BaseExp` **无** targetSamples 字段 | DIR-01 要求后台新增字段 |
| GAP-11 | 实验状态枚举 | 五态：待启动/运行中/已暂停/已完成/异常 | 后台新旧代码分别为 5 态与 3 态（待启动/运行中/已结束） | Q-02 待确认后统一 |
| GAP-12 | 互动关系数据源 | "当前回传数据中尚无此结构，为扩展项" | 后台最新版智能体配置已含 `relationships[{targetCode,type}]` | DIR-04 更新为配置 + 回传抽取双源 |
| GAP-13 | 预警规则列举 | 4 条（含"回传量异常下降"） | 面板标题仅列 3 条 | FR-IN-01 列全 4 条并驱动扫描 |
| GAP-14 | 任务模式未呈现 | 功能清单要求"政策回溯/未来预测" | 后台已有 `taskMode`/`snapshot`，驾驶舱未展示 | FR-EX-01 增加实验属性栏 |
| GAP-15 | 网络图节点坐标 | — | 25 个节点坐标**硬编码**（`NETWORK_NODES`） | FR-AG-02 要求自动布局 |
| GAP-16 | 时间与同步状态 | — | 顶部时间硬编码 `2026-08-02 12:03:45`，"数据同步中"为静态标签 | FR-G-02/FR-G-03 |

---

## 三、术语与统计口径（全文强制统一）

| 术语 | 定义 |
| --- | --- |
| 智能体 | 后台配置的虚拟实验参与者，编号以 A/B/C/D 开头（A 中青年家庭、B 老年、C 新市民、D 单身青年），档案持久、可跨实验复用 |
| 真人参与者 | 通过实验端参与的真人与游客；编号形如 `P-014`；名称规则：填过个人信息问卷 → 真实姓名，未填 → `游客XX` |
| 记名样本 / 游客样本 | 个人信息问卷填了姓名的为记名；否则为不记名游客。**游客仅计入总体指标，不参与群体维度分析** |
| 四个群体 | 中青年家庭、老年群体、新市民、单身青年（必须与后台智能体 `group` 口径一致，也是协商博弈的分组依据） |
| 有效样本 | 通过质量校验的记录（剔除重复提交、必填项缺失），校验规则由后台定义 |
| 回收率 | 真人实验有效回收样本数 ÷ 实验配置的目标样本数（**单一口径，全站一致**） |
| 支持率 | 当前轮次中最终获胜方案得票数 ÷ 全部有效投票数 |
| 贡献度 | 发言贡献(30%) + 投票参与(30%) + 互动影响(40%)，三项各归一化 0–100 后加权；互动影响缺失时降级为发言贡献与投票参与各 50% |
| 实验前 / 实验后 | 实验前 = 第 1 轮基线值（或实验前问卷基线）；实验后 = 末轮（或回收结束值） |
| 回收截止 / 结果归档 | 截止：停止接收数据、对参与者关闭入口（数据定格）；归档：清洗无效样本、计算最终聚合、生成档案与报告、锁定数据 |
| 满意度（三套口径 + 输入/输出分离，禁止混用） | ①**配置初值**：智能体档案"满意度"字段（属仿真参数），实验期间只读、不可修改；②**逐轮读数**：协商博弈中引擎每轮产生的主观感受过程数据，驾驶舱只读展示、**不回写配置**；③**社区满意度指数**：独立居民问卷加权评分（0–100）。若要做"满意度随投票修正"，须先定性为**引擎机制**（参数冻结进实验配置、报告声明内生变量）或**事后分析指标**（只在分析侧计算，不进入引擎） |
| 本场 / 历史场次汇总 | 协商博弈实验可由多位真人各发起一个议题、独立成场；本场 = 单场结果，汇总 = 全部已完成场次的跨场聚合 |

---

## 四、全局需求（FR-G）

| 编号 | 需求 | 明细要求 | 现状 | 优先级 |
| --- | --- | --- | --- | --- |
| FR-G-01 | 应用框架 | 顶部指挥栏（日期时间 + 主标题 + 中英副标题 + 数据同步状态）+ 居中切角导航（5 项：总览/社区画像/实验画像/智能体/洞察）+ 主区 + 页脚（"共同富裕感知实验 · 数据分析驾驶舱" / "实验数据 · 只读分析端"） | 已实现 | P0 |
| FR-G-02 | 实时时钟 | 顶栏展示真实当前日期时间（现为硬编码），秒级刷新；大屏常驻不漂移 | 未实现 | P0 |
| FR-G-03 | 数据同步状态 | 展示"数据同步中/同步正常/同步中断"真实状态，中断时全局可见提示并可点击查看原因 | 静态标签 | P0 |
| FR-G-04 | 只读约束 | 全站无写入操作（唯一例外：预警处理状态回写）；不提供实验配置入口 | 已实现 | P0 |
| FR-G-05 | 自适应与投屏 | ≥1366×768 正常可用；1920×1080 为最佳；适配 2K/4K 缩放与浏览器 100%–125% 缩放；平板/手机可只读浏览（关键表格横向滚动，不破版） | 部分实现 | P0 |
| FR-G-06 | 动效降级 | 尊重 `prefers-reduced-motion`，关闭扫描线、脉冲、页面入场动画 | 已实现 | P1 |
| FR-G-07 | 空态与异常态 | 每个数据板块具备 加载中 / 无数据 / 接口失败（可重试） 三态；单板块失败不影响整页 | 未实现 | P0 |
| FR-G-08 | 口径可追溯（不在界面常驻） | 指标口径统一在《数据指标来源说明.md》维护并随版本更新；**界面不再常驻展示"口径说明"小字**，如需可按需以 `?` 图标/tooltip 展开，不占用图表下方空间 | 已按新口径移除 | P0 |
| FR-G-09 | 页面切换 | 切页保留筛选上下文（如已选实验、已选社区）；支持 URL 直达（深链）用于汇报投屏 | 未实现 | P1 |
| FR-G-10 | 命名映射 | 统一"后台 category ↔ 驾驶舱展示名"映射表并在文档/代码中固化（见 1.2） | 部分实现 | P0 |
| FR-G-11 | 文字密度控制 | 界面只保留**指标名、数值、单位、图例、状态词与必要操作提示**；不展示洞察、结论、建议、口径说明等叙述性段落。分析性文字统一放到《数据指标来源说明.md》与"一键生成报告"中 | 已按此口径清理 | P0 |

---

## 五、总览页需求（FR-OV）

### 5.1 实时回传播报条（FR-OV-01）

- 需求：单行无缝滚动，展示最新逐笔回传记录；每条含 **回传时间 + 参与者姓名 + 实验类型 + 动作内容**。
- 内容模板（按类型）：
  - 协商博弈：`【协商博弈】智能体 A02 王晓燕 第 6 轮 支持方案C·混合功能区` / `反对：配送站太吵`
  - 行为决策：`【行为决策】游客108 完成第 3 轮，购买 2 瓶，公益 ¥0.5`
  - 意愿采集：`【意愿采集】游客31 选择 参保（动态保费 ¥18.6）`
- 规则：姓名显示遵循记名/游客规则；新记录到达时无需刷新页面；滚动循环不得出现空白断档（Demo 采用双轨复制实现）。
- 数据来源：过程回传记录（流式）。
- 验收：新回传出现延迟 ≤60s（前端 30s 轮询增量，不依赖推送）；连续运行 24h 无卡顿/内存增长。
- 优先级：P0。

### 5.2 KPI 卡片带（FR-OV-02）

四张卡，每张含图标、标签、主数值、副说明、迷你趋势线：

| 卡片 | 主值 | 副说明 | 口径 |
| --- | --- | --- | --- |
| 实验总量 | 后台实验总数 | `运行中 X · 已结束 Y` | 按实验状态拆分 |
| 累计参与 | 参与过实验的全部人数 | `真人 Y`（**不拆分展示智能体数量**） | 智能体参与者与真人参与者各自去重后相加 |
| 数据回传 | 当日回传记录条数 | `较昨日 ±Z%` | 与昨日**同一时段**环比 |
| 结果回收率 | 真人实验回收率 | `有效样本 X/Y` | 有效样本 ÷ 目标样本数 |

- 待确认：KPI 是否为全局口径（当前 Demo 为全量），是否需要按社区/时间范围筛选（Q-04）。
- 优先级：P0。

### 5.3 实验运行全景表（FR-OV-03）

表头：`编号 | 实验 | 类型 | 社区 | 状态 | 最新结果 | 数据来源 | 参与/场次 | 详情入口`

| 列 | 口径 | 来源 |
| --- | --- | --- | --- |
| 编号 / 实验 / 状态 | 直接取后台实验配置 | 实验配置 |
| 类型 | 后台 category 映射展示名（SP/CW/YL 前缀区分） | 实验配置 |
| 社区 | 实验所属社区样板（后台 `communityId` → 社区名） | 实验配置 + 社区样板 |
| 最新结果 | 按类型展示核心结果指标 + 进度条（协商博弈=支持率；行为决策=捐赠意愿率 + 人均捐赠；意愿采集=参保意愿） | 聚合结果 |
| 数据来源 | 协商博弈=`智能体多轮博弈`；其余=`真人输入` | 由类型决定 |
| 参与/场次 | **协商博弈=累计场次数（显示为"N 场"，与"历史场次汇总"同口径，不按人数计数）**；行为决策/意愿采集=该实验去重参与人数 | 场次归档 / 回传记录 |
| 详情入口 | 点击/箭头进入该实验的"实验画像页"（深链） | FR-G-09 |

- 排序：默认运行中在前、按更新时间倒序；支持按状态/类型/社区筛选与关键字搜索（Demo 未实现，列为 P1）。
- 空数据：展示"暂无实验"，不显示空表头。
- 优先级：P0。

### 5.4 实验参与与回收情况（FR-OV-04）

- 三行统一按**回收口径**展示，格式：`实验编号 有效回收`、进度条、`已回收/目标`、备注：
  - `SP-001 有效回收` = 22/25（协商博弈：**有效回收份数 / 配置的智能体视角数**，与其余两行同格式、同口径、不带备注；具体口径待确认见 Q-14）；
  - `CW-001 有效回收` = 126/137；`YL-002 有效回收` = 89/98。
- 底部汇总行：`真人目标样本 X 份 · 已回收有效 Y 份`、`真人实验回收率 Z%` + 环形进度；**回收率仅按真人实验（CW/YL）的问卷与行为数据计算**。
- 面板右上角标注口径标签（回收口径），并给出协商博弈行统计范围的口径说明。
- 场次维度的指标（累计场次、通过率、平均最终支持率、平均讨论轮数）仅在实验画像页"历史场次汇总"中呈现，总览页不重复展示。
- 优先级：P0。

### 5.5 实验类型最新结果对比（FR-OV-05，缺口补齐）

- 需求：取每类实验各自核心结果指标的**最新值**并列展示；因三类指标口径不同，**不做跨类型同指标比较**，仅做管理性并列。
- 展示：三张并排小卡（类型 / 实验编号 / 指标名 / 值 / 时间戳）。
- 优先级：P0（补齐 GAP-01）。

---

## 六、社区画像页需求（FR-CM）

数据来源为**社区样板的独立基础数据**（不随实验回传），围绕所选社区展示四维度。

### 6.1 社区切换（FR-CM-01）

- 顶部社区切换（按钮组或下拉），展示 `社区名 · 类型标签`（如"杨柳郡未来社区 · 社区样板"）；页面全部内容随切换联动。
- 当前仅 1 个社区时必须可用，并提示"新增社区后此处可切换"。
- 数据来源：后台社区样板列表（`CommunityTemplate: id/name/typeTag/desc`）。
- 优先级：P0。

### 6.2 人口结构（FR-CM-02）

- 展示四个群体占比（横向条形 + 百分比），群体口径必须与智能体配置一致。
- 补充三项基础指标：总人数、户均规模、老龄化率。
- 数据来源：社区人口台账/居民登记信息；群体划分口径 = `CommunityTemplate.population[{group, ratio}]`。
- 优先级：P0。

### 6.3 公共服务设施构成（FR-CM-03）

- 展示五类设施数量：基本公共服务、便民商业服务、公共空间、职能管理、居民楼。
- 底部：设施总数、基本公共服务占比（计算值）。
- 数据来源：社区设施台账；口径 = 社区样板 `buildings[]` 按建筑 `category` 分组计数（与协商博弈的建筑类目保持对应）。
- 优先级：P0。

### 6.4 民生福祉（FR-CM-04）

- 满意度指数（0–100，居民对社区各项服务评分的加权平均）。
- 当前第一诉求 + 核心诉求 TOP5：**按相对排序展示，不标注百分比**（排序反映关切程度，不做伪精确量化）。
- 数据来源：独立于实验的周期性居民问卷调查（开放题 + 诉求勾选题频次前五）。
- 优先级：P0。

### 6.5 环境治理（FR-CM-05）

- 三项静态指标：绿地率、容积率、15 分钟生活圈覆盖率。
- 数据来源：规划部门静态指标；覆盖率可由设施数据 + 地图可达性分析计算。
- 优先级：P0。

---

## 七、实验画像页需求（FR-EX）

### 7.1 实验选择与属性（FR-EX-01）

- 右上悬浮"实验选择下拉"，选项格式 `编号 · 实验名`；切换后页面全部内容联动。
- 实验属性栏（缺口 GAP-14 补齐）：编号、名称、类型、所属社区、状态、任务模式（政策回溯/未来预测）、模型引擎、推演快照版本与生成时间、轮次配置（已执行/配置值）。
- **展示对象稳定性（重要）**：实验的启停、状态变化、后台新增/归档实验，**不得自动改变当前展示对象**；用户选中的实验保持选中，直到用户手动切换。被停止/已归档的实验仍以其历史数据只读展示，并在属性栏标注状态（不得因列表刷新而跳到另一个实验）。
- 若被测实验是"实验族"（一个实验下含多个并行运行的子实验/版本），则切换粒度到**实验族**，族内子实验在页面内并列展示（见 7.4），不通过顶部下拉逐个切换。
- 数据来源：实验配置（`BaseExp` + 类型专属字段）。
- 优先级：P0（属性栏 P1）。

### 7.2 实验档案 · 生命周期时间轴（FR-EX-02）

统一**三节点**，切换实验联动，**只依赖实验起止时间与聚合进度，不依赖逐轮实时推送**：

- 实验启动（显示启动时间，来自实验配置）→ 进行中（协商博弈：`当前轮次/配置轮次 · 当前支持率`；行为决策与意愿采集：`已回收份数/目标份数`）→ 结束归档（显示结束时间、归档时间）。
- 状态视觉：已完成（青）/ 进行中（金 + 脉冲）/ 未开始（灰）。
- 不做轮次里程碑（第1轮/第3轮/中期评估均不作为节点），时间轴不标注逐轮支持率快照；逐轮细节统一由 7.3 的逐轮趋势图承担（属历史数据拉取，非实时）。
- 可选节点：回收截止时间（后台提供该字段后再加），并与结束归档区分——截止=停止收数、对参与者关闭入口；归档=清洗定稿、锁定数据不再修改。
- 说明：生命周期只需"开始时间 + 结束时间 + 当前进度"三项数据，不需要任何轮次级事件流。
- 数据来源：实验配置（启动时间、状态、结束/归档时间）+ 聚合进度（当前轮次、回收份数）。
- 优先级：P0。

### 7.3 协商博弈实验结果面板（FR-EX-03）

**本场 / 历史场次汇总 视图切换**（"查看范围"开关）：

A. 本场视图
1. 逐轮支持率趋势：5 条曲线（全体 + 四群体）按轮次排列。
2. 关键指标卡：当前支持率、目标参与（实到/目标）、完成轮次（当前/配置）、累计发言条数。
3. 运行约束标签（必须呈现，保持一行）：**单实验独占执行 · 排队启动**；详细规则（并发限制、启动后锁定、暂停后变更）写在文档中，界面不展开。
4. 最新一轮立场分布：同意 / 中立 / 反对 三分类占比（只出数值，不展示结论/洞察文字，见 FR-G-11）。
5. 质性信息（缺口 GAP-07 补齐）：**理由归集与代表性发言**——按立场分组展示智能体发言原文摘录（含所属群体、轮次）。
6. 实验前后对比：按全体 + 四群体给出 基线 → 当前 的条形对比与差值（如 `全体 +14pp`），只出数值不出结论句。
7. 界面不呈现口径与叙述性文字（见 FR-G-08、FR-G-11）；支持率等口径在《数据指标来源说明.md》中维护。

B. 历史场次汇总视图
1. 汇总卡：累计场次、议题通过率（过半场次数/总场次）、平均最终支持率、平均讨论轮数（口径：3–5 轮由发起者决定）。
2. 最终支持率分布：区间桶 `<50% / 50–60% / 60–70% / ≥70%`，`<50%` 视为未过半并用告警色。
3. 议题类型通过情况：议题聚类（设施便民类 / 空间改造类 / 运营管理类）与各自通过比（**不附洞察文字**，见 FR-G-11）。
4. 历史场次明细表（按时间倒序）：场次编号、议题、发起人（可为"游客XX"）、讨论轮数、最终支持率（含迷你进度条）、结果（通过/未过半）。

- 数据来源：个体实验结果（`SpatialResult: votedCandidate/stance/quote/totalResources/rounds`）+ 回传记录 + 获胜方案判定。
- 优先级：P0。

### 7.4 行为决策实验结果面板（FR-EX-04）

1. 结果总览 4 卡：参与人数、有效回收、愿意捐赠人数与占比、公益金额合计。
2. 关键结果指标：捐赠意愿率（核心）、人均捐赠、单笔最高、回收率。
3. 三子实验横向对照表：列为 `子实验 / 参与·有效 / 捐赠意愿率 / 人均捐赠 / 公益金额 / 状态（运行中）`，数据取自同一份子实验数据源。**不再另设对比条与子实验卡片**，避免同一组数字在多处重复。
4. **三个子实验与对照口径（已确认）**：子实验为 `CW-0001 爱心溢价效应` / `CW-0002 爱心撬动效应` / `CW-0003 爱心锚定效应`，由同一实验启动后**随机分配**给参与者、共同运行，构成**三臂随机试验**；**三者均为实验组（干预条件），本实验无对照组**。因此界面并列展示三臂捐赠意愿率，**不出现"对照组 / 较对照组"字样**；如需对照，须在实验设计中另设无干预条件。跨社区对照实验启动后此表自动扩展。
5. 界面不呈现口径与洞察文字（见 FR-G-08、FR-G-11）；捐赠意愿率、公益金额合计、人均捐赠的口径在《数据指标来源说明.md》中维护。
6. **版本与分组（已收敛）**：后台 `BehaviourExpVersion`（v1 / v2 / v3a / v3b）仅在启用多版本设计时使用；本实验已确认**子实验内部不设二级分组**（v3a / v3b 不启用），因此不出现"子实验 × 组别"的二维表（Q-05、Q-17 已收敛）。
7. **子实验归属与运行方式（已确认）**：CW-001 为**一个实验**，内含三个子实验；实验启动后由实验端**随机分配参数**（参与者随机进入其中一个子实验），三者**并行运行**、同属该实验。因此本页按实验族展示——下拉只选 CW-001，三个子实验并列呈现，**不使用"切换实验"逐个查看，也不存在按子实验单独启停**；后台需记录每位参与者的子实验归属，回传与个体结果带子实验标识（Q-16 已确认，见 DIR-05）。
8. **"对照组"的判定原则（重要）**：是否构成对照组由**设计**决定，不由名称决定——①该条件是否施加干预（无干预＝控制条件）；②参与者是否随机分配（保证组间可比）。**本实验三个子实验均为干预条件，因此不存在对照组**；仅修改子实验名称不会产生对照组，必须新设无干预条件并随机分配。驾驶舱只按实际设计呈现，不虚构对照关系。

- 说明：本类型**不呈现**实验前后对比（无多轮博弈数据）。
- 优先级：P0。

### 7.5 意愿采集实验结果面板（FR-EX-05）

1. 结果总览 4 卡：参与人数、有效回收、愿意参保人数与占比、意愿保费中位数（或均值口径二选一，需统一）。
2. 关键结果指标：参保意愿比例（核心）、有效样本 `有效/目标`、可接受保费均值、回收率。
3. 金额类指标补齐（GAP-09）：总缴纳保费、总投保金额、平均可接受保费。
4. **分层呈现（重要口径）**：
   - 第一层：全样本总体意愿（记名 + 不记名，标注总样本量）。
   - 第二层：群体意愿（**仅记名居民样本**，标注 n 值与"游客样本不参与分组"提示），按四群体展示。
   - 小样本（如 n<50）需给出"样本量较小仅供参考"提示。
5. 问卷题汇总：问卷中"是/否"类题目（是否愿意参保、是否接受保费浮动、是否推荐给亲友等）的作答人数统计（是/否计数）。
6. 群体偏差突出项由**洞察页的预警卡片**承载，本页不展示洞察文字（见 FR-G-11）。
7. 界面不呈现口径文字：不记名游客只计总体、群体维度仅记名样本等口径在《数据指标来源说明.md》中维护（见 FR-G-08）。

- 优先级：P0。

### 7.6 对比分析（FR-EX-06）

| 对比类型 | 适用范围 | 口径 | 现状 |
| --- | --- | --- | --- |
| 实验前后对比 | 仅协商博弈（有逐轮数据） | 实验前=第1轮基线；实验后=末轮；计算同一指标差值（pp） | 已实现 |
| 实验组 vs 对照组 | **仅当同一次随机试验内存在真实对照条件时成立**（行为决策：同一子实验内部的随机分组；其余实验需设计时设置对照组）。三个子实验之间**不成立**——它们均为处理组 | 同一次随机试验内：处理组 vs 对照组的核心指标对比 | 需按此口径调整（Demo 原将 c 标为"对照组"，已改为并列处理组） |
| 跨社区横向对比 | 同一实验配置在不同社区样板运行后 | 同指标跨社区并列；**当前仅 1 个社区，界面为预留说明，新增社区并重复实验后自动开放**（不得显示假数据） | 未实现（GAP-03） |
| 三实验横向对比表 | 全类型 | 因核心指标口径不同，不做跨类型同指标比较；并列"核心结果指标 + 参与规模 + 有效回收 + 回收率" | 未实现，P1 |

- 优先级：跨社区 P1，三实验对比表 P2。

---

## 八、智能体页需求（FR-AG）

**数据边界：本页所有数据仅来自协商博弈实验。** 行为决策与意愿采集的参与者为真人，不涉及智能体；其参与者的主观感受在各自实验画像中呈现。

### 8.1 智能体档案（FR-AG-01）

档案表列（默认按贡献度倒序，滚动查看全部，表头吸顶）：

`编号 | 姓名 | 性别 | 年龄 | 职业 | 户籍 | 群体 | 角色 | 满意度 | 说服敏感度 | 贡献度`

- 角色：代表 / 个体（代表需视觉高亮）。
- 满意度色阶：≥70 青、55–69 中、<55 告警色。
- 贡献度：数值 + 迷你条，脚注给出计算口径与降级规则。
- 数据来源：后台智能体配置（基本属性/个人画像/仿真参数）+ 回传统计（发言数、得票数、资源获取）。
- 优先级：P0。

### 8.2 智能体资源流动（FR-AG-02，缺口补 GAP-04）

- 需求：按轮次展示四群体的**累计资源获取**曲线或堆叠图，体现资源在群体间的不均衡；支持按个体查看其资源获取轨迹。
- 数据来源：协商博弈回传的累计资源（`SpatialResult.totalResources` + 逐轮增量）。
- 说明：Demo 中 `AGENT_RESOURCE_TREND` 常量已存在但未渲染，需落地为可用图表。
- 优先级：P1。

### 8.3 智能体参与历史（FR-AG-03，缺口补 GAP-06）

- 档案行悬停/点击弹出该智能体参与过的实验列表及在其中扮演的角色（代表/个体）、参与轮次、投票与发言概要。
- 数据来源：用户与智能体的参与记录（后台）。
- 优先级：P1。

### 8.4 智能体标签（FR-AG-04，缺口补 GAP-05）

- 由后台根据贡献度排名与立场稳定性自动打标：**意见领袖**（贡献度靠前）、**立场坚定**（轮间立场变化小）、**可说服**（立场易受他人影响）。
- 展示：档案表/详情卡中的标签胶囊；标签规则与阈值需在脚注说明。
- 优先级：P1。

### 8.5 智能体动态 · 互动网络（FR-AG-05）

- 节点 = 智能体（代表节点放大且金色，节点大小反映关联数量）；边 = 互动关系：**友好（附议/支持，青）/ 中立（灰）/ 对立（反对，红）**，有向边。
- 交互：悬停档案行 → 网络中该节点及一阶关系高亮，其余降透明度。
- **布局必须自动生成**（力导向或按群体分区布局），不得硬编码坐标（GAP-15）；节点数随实验参与者变化自适应。
- 图例与"N 人 × M 条有向关系"统计需动态计算。
- 数据来源：智能体配置 `relationships`（友好/中立/不友好）+ 后台从发言与投票记录抽取的互动关系（双源合并，需去重与优先级规则）。
- 优先级：P0（网络图与联动），P1（布局算法化）。

### 8.6 满意度追踪（FR-AG-06）

- 按四群体分别统计**逐轮满意度**曲线（口径：智能体每轮博弈结束后的评价输出聚合，属仿真值）。
- 明确标注与社区画像"居民满意度指数"为两套口径、不可混用。
- 扩展（P2）：同一实验体系下有多个实验时，扩展为同体系跨实验对比。
- 优先级：P0。

### 8.7 行为模式 · 立场演变（FR-AG-07）

- 取协商博弈中各群体支持率逐轮曲线，识别立场类型（如"立场坚定型""可说服型"），以**标签**形式标注在曲线区，不输出建议段落（见 FR-G-11）。
- 分析性结论由"实验分析报告 · 一键生成"承载（FR-IN-03）。
- 优先级：P0。

---

## 九、洞察页需求（FR-IN）

### 9.1 异常预警与提示（FR-IN-01）

规则（后台按固定规则自动扫描，4 条全量呈现，缺口 GAP-13）：

| 规则码 | 规则 | 阈值 |
| --- | --- | --- |
| R1 | 参与率低于阈值 | 实际参与人数 < 目标样本的 80% |
| R2 | 群体偏差过大 | 某群体核心指标偏离全体均值 > 20 个百分点 |
| R3 | 相邻轮波动过大 | 协商博弈支持率轮间变化 > 15 个百分点 |
| R4 | 回传量异常下降 | 当日回传条数较昨日明显减少（阈值可配） |

- 卡片信息：级别（预警/提示）、实验编号、触发时间、描述、处理入口。
- 视觉：预警 = 珊瑚红左侧色条 + 图标；提示 = 琥珀金。
- 处理：点击"处理"记录处理人/时间/备注并回写状态（**当前 Demo 无回写逻辑，需实现**）；已处理预警移入历史或降低强调。
- 列表支持按级别/实验/时间筛选与分页。
- 优先级：P0。

### 9.2 实验分析报告 · 一键生成（FR-IN-03）

- 流程：选择实验 → 点击"AI 生成报告" → 展示生成进度（分步：读取实验配置与参与记录 / 聚合过程与结果 / 汇总预警记录 / 生成结论与建议）→ 输出结构化报告。
- **报告结构（七节，缺口 GAP-08）**：①实验概况 ②核心指标卡 ③逐轮过程分析 ④群体维度分析 ⑤质性结论（代表性发言）⑥异常与预警记录 ⑦综合结论与政策建议。
- 报告不得产生新的数据口径，全部内容来自前述各节的数据聚合（可注明数据截取时间与样本量）。
- 支持导出（PDF/Word 至少其一；Demo 的"导出报告"按钮无实现，需落地），导出内容含图表快照与口径说明。
- 生成失败/超时需给出可重试提示；报告生成过程不得阻塞其他页面。
- 优先级：P0（生成+展示），P1（导出）。

---

## 十、数据需求（DIR）

### 10.1 数据链路（四类记录）

1. **实验配置信息**：编号、名称、类型、状态、目标样本数、轮次配置、所属社区、实验条件分组（对照组开关）；类型专属参数：
   - 协商博弈（spatial）：地图尺寸、资源点、移动步数、单轮时长、资源再生、建筑集合、智能体集合、模型引擎与参数、任务模式（政策回溯/未来预测）、发起方、候选点数量、实验主题、推演快照；
   - 行为决策（charity）：初始资金、统一背景描述、实验版本（v1/v2/v3a/v3b）与分组（A/B）、是否展示他人选择；
   - 意愿采集（insurance）：基础保费、赔付倍数、风险概率、互助金比例、最高投保额、投保期数。
2. **过程回传记录**：实验者每轮/每笔动作明细（发言、投票、购买、捐赠、问卷作答）+ 时间戳 + 轮次；是实时播报、逐轮趋势、回收统计的来源。
3. **个体实验结果**：实验结束后每人一份（协商博弈：投票方案/立场/代表发言/累计资源/参与轮次；行为决策：累计购买次数/总购水量/公益金额/实际支付/平均每轮购水；意愿采集：是否愿意参保/总保费/总投保金额/理赔次数/问卷作答明细）。
4. **用户与智能体信息**：真人用户参与记录与个人信息问卷（区分记名/游客）、智能体配置档案（群体、仿真参数、关系、校准状态）。

社区画像页的人口、设施、满意度、环境数据为社区样板独立基础数据，**不随实验回传，需单独维护**。

### 10.2 指标口径字典（关键项）

| 指标 | 口径 | 来源 |
| --- | --- | --- |
| 实验总量 | 后台实验总数（按状态可拆） | 实验配置 |
| 累计参与 | 智能体与真人各自去重后相加 | 回传 + 用户表 |
| 数据回传量 | 当日回传记录条数；环比 = 与昨日同时段比较 | 回传记录 |
| 结果回收率 | 有效样本 ÷ 目标样本数（真人实验） | 回传 + 实验配置 |
| 支持率 | 获胜方案得票 ÷ 有效投票 | 个体结果 |
| 逐轮支持率 | 每轮回传记录中的当轮支持率 | 回传记录 |
| 分群体支持率 | 同一口径按四群体分别计算 | 个体结果（group） |
| 立场分布 | 同意/中立/反对三分类占比 | 个体结果（stance） |
| 捐赠意愿率 | 捐赠额 > 0 的人数 ÷ 有效参与人数 | 个体结果 |
| 人均捐赠 | 公益金额合计 ÷ 捐赠人数 | 个体结果 |
| 参保意愿比例 | "愿意参保"有效样本 ÷ 有效回收样本 | 个体结果 |
| 平均可接受保费 | 问卷金额字段均值（与"中位数"口径二选一，需统一） | 个体结果 |
| 贡献度 | 发言贡献 30% + 投票参与 30% + 互动影响 40% | 回传记录统计 |
| 议题通过率 | 最终支持率过半场次数 ÷ 总场次数 | 场次归档 |

### 10.3 接口需求

| 接口 | 说明 | 实时性要求 |
| --- | --- | --- |
| 实验列表与配置 | 支持按社区/状态/类型筛选，含目标样本数与类型专属字段 | 1–5 min |
| 回传明细（增量） | 按游标/`since` 拉取最新记录（分页，默认 20 条），供播报条；**无需流式推送通道** | 30–60s 轮询 |
| 生命周期进度 | 实验启动时间、结束/归档时间、当前轮次、已回收份数（一次 GET 返回） | 1–5 min |
| KPI 聚合 | 四张 KPI + 环比 | 30–60s |
| 实验聚合结果 | 按实验 + 类型返回全部结果面板所需数据（含分群体、逐轮、版本/分组） | 1–5 min |
| 场次列表 | 协商博弈历史场次（议题/发起人/轮数/最终支持率/是否通过） | 5 min |
| 智能体档案与动态 | 档案字段 + 贡献度 + 互动关系 + 逐轮满意度与资源 | 5 min |
| 社区画像 | 人口/设施/满意度/环境四维度 | 日级 |
| 预警记录 | 列表 + 状态回写 | 1–5 min |
| 报告生成 | 提交任务 + 查询进度 + 获取结果 + 导出 | 异步任务 |

- 统一约定：返回体含 `数据时间戳` 与 `数据是否完整（未归档/已归档）`；未归档数据必须在 UI 标注"进行中/未定稿"。
- 失败处理：超时重试、降级为上次缓存值并展示"数据可能不是最新"。

### 10.4 上游字段缺口（需后台配合改造）

| 编号 | 缺口 | 需求 |
| --- | --- | --- |
| DIR-01 | `BaseExp` 无"目标样本数" | 新增 `targetSamples`，否则回收率无法计算（GAP-10） |
| DIR-02 | 实验状态枚举不一致（3 态 / 5 态） | 统一为 待启动/运行中/已暂停/已完成/异常（待确认，Q-02） |
| DIR-03 | 回传记录缺"立场标签、发言原文、发言间互动关系、逐轮满意度" | 协商博弈回传结构化扩展，用于质性信息、互动网络与满意度追踪 |
| DIR-04 | 互动关系来源 | 明确以智能体配置 `relationships` 为主、发言抽取为辅；两者冲突时的优先级规则需定义 |
| DIR-05 | 行为决策版本与分组 | 回传与个体结果需带 `versionId` + `group`，供驾驶舱按版本/分组聚合 |
| DIR-06 | 记名/游客标记 | 回传与个体结果需带 `surveyed` 标记，供姓名显示与分层统计 |
| DIR-07 | 预警记录 | 需返回规则码、阈值、实测值，供驾驶舱展示可解释信息 |
| DIR-08 | 报告 | 需明确由后台生成（聚合 + 大模型叙述）；驾驶舱仅提交与展示 |
| DIR-09 | 实验生命周期时间字段 | 需提供 `startedAt`、`endedAt`、`archivedAt`（`receiveClosedAt` 回收截止时间可选），供三节点时间轴；**不需要任何轮次级事件流** |

### 10.5 实时性分级与接口最小集（降低对接复杂度）

驾驶舱为拉取式只读端，**除播报条外不需要实时通道**，按下表三档实现即可：

| 档位 | 数据 | 刷新方式 | 接口形态 |
| --- | --- | --- | --- |
| 近实时 | 最新回传记录（播报条） | 前端 30s 轮询增量（带游标） | `GET /submissions?since=<cursor>&limit=20` |
| 准实时 | KPI、实验列表与状态、生命周期进度、预警 | 1–5 min 轮询 | `GET /kpis`、`GET /experiments`、`GET /alerts` |
| 离线聚合 | 结果面板、逐轮趋势、场次汇总、智能体档案与动态、社区画像、报告 | 打开页面时拉取，缓存 5–30 min | `GET /experiments/{id}/report-data` 等 |

说明：

1. 逐轮支持率、逐轮满意度、立场演变、资源流动等"按轮次的数据"属于**历史聚合**——后台收到回传后落库聚合，驾驶舱一次性拉取，**不需要逐轮推送**。
2. 生命周期时间轴只需"开始时间 + 结束时间 + 当前进度"三项（见 FR-EX-02），不需要轮次级事件流。
3. 接口最小集：`GET /experiments`、`GET /submissions?since=`、`GET /experiments/{id}/report-data` 三个接口即可支撑五个页面，其余接口为可选扩展。
4. 若后续要提升播报实时性，只需把轮询换成 SSE/WebSocket，页面结构无需改动。

---

## 十一、非功能需求（NFR）

| 编号 | 类别 | 要求 |
| --- | --- | --- |
| NFR-01 | 实时性（拉取式，不依赖推送） | 播报条：前端 30s 轮询增量取最新记录，≤60s 可见；KPI ≤60s；实验状态与进度 ≤5min；社区画像日级。**全站不要求 WebSocket/SSE 推送通道**；后台后续若提供推送可无缝升级，页面结构不变 |
| NFR-02 | 性能 | 首屏可交互 ≤3s（内网）；页面切换 ≤300ms；25 节点网络图与折线图渲染 ≤100ms；单表 500 行内不卡顿，超出采用分页/虚拟滚动 |
| NFR-03 | 稳定性 | 大屏 7×24 常驻：自动刷新、断线重连、内存不增长、动画不累积 |
| NFR-04 | 兼容性 | Chrome/Edge 最近两个大版本（大屏一体机）、Safari（iPad 只读）；不支持 IE |
| NFR-05 | 显示适配 | 主投屏 1920×1080 3 米观距可读（正文字号不小于基准 12px 并按大屏模式等比放大）；支持 4K 缩放 |
| NFR-06 | 可用性 | 只读不可误操作；关键指标可下钻；所有图表有图例与单位；空态/异常态文案明确 |
| NFR-07 | 可访问性 | 支持 `prefers-reduced-motion`；红/青对立色需辅以形状或文字标签（色盲友好） |
| NFR-08 | 安全与合规 | 内网部署、页面 `noindex,nofollow`、不在前端暴露个人信息明细（记名数据仅按统计口径展示）、接口鉴权与最小权限、操作留痕（预警处理） |
| NFR-09 | 可维护性 | 单文件 1367 行需拆分为 页面/组件/图表/数据服务 四层；mock 常量集中替换为接口调用；统一 TypeScript 类型（与后台上游类型共享或对齐） |
| NFR-10 | 可观测 | 前端错误上报、接口失败率与耗时监控、数据延迟告警 |

---

## 十二、优先级与里程碑建议

| 阶段 | 目标 | 范围 |
| --- | --- | --- |
| M1 · 可用版（P0） | 五页与验收点对齐 + 接入真实数据 | FR-G-01~05、G-07~08、G-10；FR-OV-01~05；FR-CM-01~05；FR-EX-01~05；FR-AG-01/05/06/07；FR-IN-01、IN-03（生成展示）；NFR-01~05 |
| M2 · 完善版（P1） | 补齐口径缺口与交互深度 | FR-EX-06 跨社区对比、FR-AG-02 资源流动、FR-AG-03 参与历史、FR-AG-04 标签、FR-IN-03 导出、FR-G-09 深链、FR-OV-03 筛选、报告七节完整落地 |
| M3 · 增强版（P2） | 扩展分析能力 | 三实验横向对比表、满意度跨实验对比、智能体校准可信度展示、AR 设备运行态势、多社区大屏轮播、告警阈值后台可配 |

---

## 十三、待确认问题（Open Questions）

| 编号 | 问题 | 影响 |
| --- | --- | --- |
| Q-01 | "目标样本数"由谁维护、按实验还是按社区默认？ | 回收率与 KPI 主值 |
| Q-02 | 实验状态枚举最终以 5 态还是 3 态为准？"已结束"与"已完成"区别？ | 全站状态展示、归档判定 |
| Q-03 | 驾驶舱展示名（协商博弈/行为决策/意愿采集）与后台 category 的映射是否正式固化？ | 类型映射与验收 |
| Q-04 | 总览 KPI 是否为全局口径？是否需要社区/时间筛选？ | 总览页交互 |
| Q-05 | 行为决策"内部小实验"以 3 组（a/b/c）还是后台 4 版本（v1/v2/v3a/v3b）为准？v3a/v3b 为组 A/B 如何呈现？ | 对比分析口径 |
| Q-06 | 互动关系以配置 `relationships` 为准还是从发言抽取？冲突优先级？ | 互动网络数据质量 |
| Q-07 | 参保"保费"用中位数还是均值？是否会同时展示？ | 指标一致性 |
| Q-08 | 预警阈值是否后台可配？处理动作是否需留痕与回写？ | 洞察页功能深度 |
| Q-09 | 报告是否允许人工编辑/锁定/版本留档？导出格式（PDF/Word/PPT）？ | 报告需求范围 |
| Q-10 | 是否引入账号与权限体系？不同角色可见范围是否不同？ | 安全与非功能 |
| Q-11 | 数据保留与"实验回溯"是否在驾驶舱范围（功能清单中该项未实现）？ | 范围边界 |
| Q-12 | 跨社区对比开放条件（同一实验配置需在几个社区跑过）？ | 对比模块上线时机 |
| Q-13 | 大屏运行环境：分辨率、浏览器、是否以 iframe 嵌入上级平台、是否需要中英双语（标题已有英文副标题）？ | 适配与国际化 |
| Q-14 | 协商博弈"有效回收"的分子与分母是什么——"有效回收份数 / 配置的智能体视角数"，还是"已完成议题流程的视角 / 总视角"？Demo 暂以 22/25 占位（FR-OV-04） | 总览回收面板的数值 |
| Q-15 | 满意度是否要"随投票结果修正"（当轮群体倾向该智能体观点则 +5、反之 −5）？若采用须先答：(a) 归属为引擎机制还是事后分析指标；(b) 对齐判定规则（与获胜方案 / 与多数立场）；(c) 步长是否固定 ±5；(d) 中立者如何处理；(e) 同轮即时生效还是滞后一轮；(f) 0–100 截断与旧值衰减；(g) 是否影响后续发言与立场（决定它是不是内生变量） | 智能体页满意度追踪的口径、实验前后/组间对比的有效性 |
| Q-16 | ~~CW 的三个子实验是三个独立实验实例，还是同一实验下的多个版本/分组？~~ **已确认：CW-001 为一个实验，内含三个子实验；实验启动后由实验端随机分配参数（参与者随机进入其中一个子实验），三者并行运行、同属该实验（模型二）**。遗留待办：后台需记录每位参与者的子实验归属，并在回传与个体结果中带子实验标识（DIR-05） | 实验画像 / 总览的展示单元（已按"实验族"口径实现）；不再需要"切换实验"查看子实验 |
| Q-17 | ~~三个子实验内部是否还有随机分组（实验组/对照组）？~~ **已确认：无二级分组。三个子实验 = 同一实验启动后随机分配到的三个条件，构成三臂随机对照试验；无干预的「无告知」为对照组，另两个为实验组**。判定原则：是否构成对照组由设计（有无干预 + 是否随机分配）决定，不由名称决定。展示采用"横向对照表加「组别」列 + 「较对照组 ±pp」差值"（已实现）。遗留待办：后台需回传每位参与者的条件归属（组别字段） | 行为决策页指标结构（已实现）；v3a/v3b 多版本分组在本实验不启用 |

---

## 十四、附录

### 14.1 Demo mock 常量 → 真实数据接口映射

| 常量（Dashboard.tsx） | 应替换为 |
| --- | --- |
| `COMMUNITIES` | 社区样板列表接口 |
| `LIVE_TICKER` | 回传明细流（SSE/WebSocket） |
| `TIMELINE_BY_TYPE` | 实验生命周期节点（由状态/轮次/回收/归档推导） |
| `KPIS` | KPI 聚合接口 |
| `EXP_OVERVIEW` | 实验列表聚合接口 |
| `ALERTS` | 预警记录接口（含规则码与状态回写） |
| `COMMUNITY_PROFILE` | 社区画像接口（人口/设施/满意度/环境） |
| `SUPPORT_TREND` | 逐轮分群体支持率时序 |
| `CW_SUB_EXPS` | 实验版本（versionId + group）统计 |
| `AGENTS` | 智能体档案 + 实验内表现（发言/得票/资源/贡献度） |
| `AGENT_RESOURCE_TREND` | 逐轮资源获取（当前未渲染，需开发） |
| `AGENT_NETWORK` / `NETWORK_NODES` | 互动关系（边来自后台，**节点坐标改为自动布局**） |
| `AGENT_SATISFACTION_TREND` | 逐轮分群体满意度 |
| `SP_SESSIONS` | 协商博弈场次列表 |
| `AI_REPORTS` | 报告生成服务（七节结构） |

### 14.2 Demo 中值得保留的产品细节（勿在开发中丢失）

1. 每个指标旁的"口径说明"文字（把统计定义写在界面上，是本产品公信力的关键）。
2. 记名/游客的分层展示与样本量标注（意愿采集页）。
3. 协商博弈的"单实验独占执行、排队启动"运行约束提示。
4. "回收截止"与"结果归档"的阶段区分说明。
5. 三类实验指标口径不同、**不做跨类型同指标比较**的原则。
6. 大屏视觉规范（配色、切角面板、扫描线、等宽数字、脉冲状态点）与动效降级。

---

（本文档基于对 `共同富裕感知实验驾驶舱-Demo_副本` 源码、《数据指标来源说明.md》、《功能清单（对标验收）（数据采集系统二期）.xlsx》及上游后台系统代码的分析整理；配套验收追踪矩阵见同目录《驾驶舱需求追踪矩阵（对标验收）.md》。）
